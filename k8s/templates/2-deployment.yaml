apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-cib-deployments
  namespace: dev-cib-analytics
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cib-analyser
  template:
    metadata:
      labels:
        app: cib-analyser
    spec:
      containers:
        - name: cib-analyser
          image: dharapat.azurecr.io/dharapat-prime-bank-cib:v1.0.6
          env:
            - name: PYTHONUNBUFFERED
              value: "1"

      imagePullSecrets:
        - name: cib-registry-key